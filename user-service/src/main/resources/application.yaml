spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/notification
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    root: INFO
    br.com.nicolas: DEBUG

server:
  port: 8080